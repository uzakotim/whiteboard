/**
 * Dashboard page component that renders a welcome message.
 *
 * @returns A JSX element containing the dashboard's welcome content.
 */
export default function DashboardPage() {
  return <div>Welcome!</div>
}